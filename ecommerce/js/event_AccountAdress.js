$(document).ready(routers);
$("#mapa").hide()
$("#BtnLogout").click(logoutFunction);


// Eventos bindeados enel Router por implementacion de SPA

//$("#account-province").change(loadLocality);// ya esta
//$("#account-SmartPhoneSubmit").click(saveDirectionUser)// ya esta
//$("#account-numberStreet").blur(searchZipCode); // ya esta



/*$("#account-numberStreet").keyup(function (e) {
    if (e.which == 13) {
        if (!$("#btnUserLogin").is(':disabled')) {
            searchZipCode()
        }
    }
});*/
